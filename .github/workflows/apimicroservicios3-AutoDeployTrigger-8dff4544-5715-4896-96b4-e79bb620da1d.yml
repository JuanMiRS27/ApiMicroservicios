name: Trigger auto deployment for apimicroservicios3

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/apimicroservicios3-AutoDeployTrigger-8dff4544-5715-4896-96b4-e79bb620da1d.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.APIMICROSERVICIOS3_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.APIMICROSERVICIOS3_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.APIMICROSERVICIOS3_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.APIMICROSERVICIOS3_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.APIMICROSERVICIOS3_REGISTRY_PASSWORD }}
          containerAppName: apimicroservicios3
          resourceGroup: ApiSencilla_group
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


